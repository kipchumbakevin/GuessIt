<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.guess.guessit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuessIt"
        android:usesCleartextTraffic="true">
        <activity android:name=".AnswerQuestion"/>
        <activity android:name=".QuestionsActivity" />
        <activity android:name=".guessing.Landing" />
        <activity android:name=".guessing.Pet" />
        <activity android:name=".guessing.Jobless" />
        <activity android:name=".guessing.African" />
        <activity android:name=".guessing.Plastic" />
        <activity android:name=".guessing.Medicine" />
        <activity android:name=".guessing.Car" />
        <activity android:name=".guessing.Student" />
        <activity android:name=".guessing.Virgin" />
        <activity android:name=".guessing.ExConvict" />
        <activity android:name=".guessing.Billion" />
        <activity android:name=".guessing.Actor" />
        <activity android:name=".Candy" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>